공개키, 개인키, 키사이즈, 모듈러 - 시스템 파라미터는 표준에 가깝도록

main01
표준 공개키 지수 e = 65537
모듈러 거듭제곱, 확장 유클리드 알고리즘 적용
printf의 서식 지정자(%llu)를 PRIu64 매크로로 변경

main02
Big Integer 산술 연산을 구현
나눗셈을 Shift-and-Subtract 기법으로 구현
ExtGCD 과정에서 d값이 음수로 떨어지지 않도록 각 변수의 부호를 추적하는 논리를 적용 - 부호 없는(unsigned) 구조체의 제약 극복
CRT 적용 - 복호화 단계에서 모듈러 공간을 p와 q로 쪼개어 연산 속도를 4배 이상 높이는 m1, m2, h 공식이 빠짐없이 들어감
BigInt_Print 출력함수로 전체 데이터 확인

main03 - 제출용
파이n 출력
안전한 버퍼 크기: MAX_WORDS를 256으로 늘려, 최대 4096비트 RSA 연산 시에도 스택 메모리가 깨지지 않도록 방어
정확한 비트 길이 마스킹: 1000비트 등 32로 나누어 떨어지지 않는 키 크기를 요청해도 상위 비트를 정확히 마스킹하여 정확한 비트 수의 난수를 생성
안전한 밀러-라빈: 판별 기준이 되는 밑수(a)를 단순 2~101이 아닌 $2 \le a \le n-1$ 범위의 거대한 랜덤 BigInt로 생성
중복 소수 방지: p와 q가 동일한 난수로 생성되는 확률 차단

full - 모든 주석

test01 - 합성수가 들어갔을 때의 실행

test02 - 기본 복호화와 CRT 복호화 비교


보안 업계에서 권장하는 RSA 키 길이 : 2048
키 길이: 32비트 워드 단위에 맞게 키우기 - 데모용이라면 64~128비트 정도, 실습/검증용이면 256비트 이상을 권장